- WRITE THE DOCU!!
- Extract memory experiment from the insert.
- Improve experiments to allow the user to specify the max length of the string and the max amount of elements inserted in the structures.

